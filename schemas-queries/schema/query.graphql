# Entry points of yours API!
type Query {
    ola: String!
    horaCerta: Date!
    userLoged: User
    destakProduct: Product
    megaSenaNumbers: [Int!]!
    users: [User]
    user(id: Int): User
    profiles: [Profile]
    profile(id: Int): Profile
}
